extends layout

block content
  #container.
    !{container}
  script(type='text/javascript' src='./js/bundle.js')
  script(type='text/javascript').
    var ui = new nudemeth.poc.web.UI();
    ui.renderClient(#{model});